[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os

# The URL is deliberately absent: env.py reads MIGRATION_DATABASE_URL from the
# environment, so migrations always run as the migrator role and there is no
# committed connection string to accidentally point at production.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
