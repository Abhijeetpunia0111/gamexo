/** Printed on every invoice/ticket. Same values as the dashboard (apps/web) uses —
 *  move this to a `/settings` API call once the backend exposes facility profile fields. */
export const FACILITY_PROFILE = {
  name: 'Navigo Sports Arena',
  addressLine: 'Survey 42, Kondapur, Hyderabad',
  pincode: '500084',
  gstin: '36AABCN1234K1Z9',
}
